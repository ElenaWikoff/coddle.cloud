import { Link } from "react-router";

const Spots = () => {
    return (
        <main>
            <p>Spots Page</p>
            <Link to="/spots/1">Spot</Link>
        </main>
    );
};

export default Spots;